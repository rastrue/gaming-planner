# Direct API URL for local dev (bypasses Vite proxy when it is unavailable).
# The backend must be running: npm run dev -w server  (or npm run dev from repo root)
VITE_API_URL=http://localhost:3001/api
